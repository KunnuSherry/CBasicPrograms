#include <stdio.h>

int main() {
    int n;
    int i;
    printf("Write the number: ");
    scanf("%d", &n);
    for(i=10; i>=1; i--){
        printf("%d X %d = %d \n", n, i, n*i);
    }
    return 0;
}
